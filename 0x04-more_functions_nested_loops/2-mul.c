#include "main.h"

/**
  * mul - Multiplies two integers
  * @a: The first number to multiply
  * @b: The second number to multiply with first number
  *
  * Return: multiply the addition of a and b
  */
int mul(int a, int b)
{
	return (a * b);
}
